import { expect } from "chai";
import { [FCName] } from "./[FCName]";

describe('[FCName]', () => {
    it('should ', () => {
        const result = [FCName]()
        expect(result).toEqual(2)
    });
});